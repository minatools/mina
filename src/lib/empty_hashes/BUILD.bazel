load(
    "@obazl_rules_ocaml//ocaml:rules.bzl",
    "ocaml_module",
)

#############
ocaml_module(
    name = "empty_hashes",
    ppx = "//bzl/ppx/exe:ppx_version",
    struct = "empty_hashes.ml",
    deps = ["@mina//src/lib/immutable_array"],
    deps_opam = ["core"],
    visibility = [
        "//src/lib/merkle_ledger:__pkg__"
    ],
)
