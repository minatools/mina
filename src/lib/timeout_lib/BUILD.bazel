load(
    "@obazl_rules_ocaml//ocaml:rules.bzl",
    "ocaml_module",
)

#############
ocaml_module(
    name = "timeout_lib",
    ppx = "//bzl/ppx/exe:ppx_jane__ppx_version",
    ppx_args = [
        # do not sort (buildifier)
        "-inline-test-lib",
        "timeout_lib",
    ],
    struct = "timeout_lib.ml",
    deps_opam = [
        "async_kernel",
        "core",
    ],
    visibility = [
        "//src/lib/block_time:__pkg__",
        "//src/lib/coda_net2:__pkg__",
    ],
)
